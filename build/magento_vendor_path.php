<?php
/**
 * We need this file in app/etc/vendor_path.php currently because otherwise composer install fails. Our goal is to remove this work around.
 * @TODO Fix composer install so we don't need this work around.
 */

/**
 * Path to Composer vendor directory
 */
return './vendor';
